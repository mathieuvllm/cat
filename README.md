# Quick explanation
This is a simplified version of the UNIX cat command, which I made just for fun.
You can only use two optional arguments:

- -E shows the newline character by printing '$'
- -n numbers the lines

Just like the original command, you can use several files as arguments.
